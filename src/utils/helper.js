export function formatNumber(num) {
    // if (num < 1000) {
    //     return num;
    // }
    // const units = ["K", "M", "B", "T"];
    // let unitIndex = 0;
    // while (num >= 1000) {
    //     num /= 1000;
    //     unitIndex++;
    // }
    // return `${num?.toFixed(1)}  ${units[unitIndex - 1]}`;
    return num;
}


